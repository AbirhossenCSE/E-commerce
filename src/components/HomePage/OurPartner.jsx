import React from 'react';
import SectionTitle from '../SectionTitle/SectionTitle';

const OurPartner = () => {
    return (
        <div>
            <SectionTitle heading='Our Partner'></SectionTitle>

            <div>Five logo And image</div>
        </div>
    );
};

export default OurPartner;